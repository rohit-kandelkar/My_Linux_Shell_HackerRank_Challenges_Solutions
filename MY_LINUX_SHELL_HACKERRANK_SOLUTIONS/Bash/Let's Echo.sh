#The echo command in Bash (and in many other shells, including PowerShell) is used to print text or variables to the terminal (or to other outputs). It's one of the most commonly used commands in shell scripting.

#CODE:
echo "HELLO"